{
  "import": [
    "moonbitlang/core/bigint"
  ],
  "targets": {
    "cpal_os_native.mbt": ["native"],
    "cpal_os_stub.mbt": ["not", "native"],
    "cpal_available_hosts_native.mbt": ["native"],
    "cpal_available_hosts_stub.mbt": ["not", "native"]
  },
  "native-stub": ["os_stub.c"]
}
