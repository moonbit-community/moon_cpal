{
  "import": ["zhengyu/moon_cpal"],
  "targets": {
    "coreaudio_native.mbt": ["native"],
    "coreaudio_stub.mbt": ["not", "native"],
    "stream_callback_bridge.mbt": ["native"],
    "stream_native.mbt": ["native"],
    "stream_stub.mbt": ["not", "native"],
    "coreaudio_configs_wbtest.mbt": ["native"],
    "coreaudio_default_config_wbtest.mbt": ["native"]
  },
  "link": {
    "native": {
      "cc": "./scripts/moon_cc.sh",
      "cc-link-flags": "-framework CoreAudio -framework CoreFoundation -framework AudioToolbox"
    }
  },
  "native-stub": ["coreaudio_stub.c"]
}
