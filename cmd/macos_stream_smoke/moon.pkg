import {
  "moonbit-community/moon_cpal/macos",
  "moonbit-community/moon_cpal",
}

supported_targets = "native"

pkgtype(kind: "executable")

options(
  targets: {
    "main_native.mbt": [ "native" ],
    "main_stub.mbt": [ "not", "native" ],
  },
)
